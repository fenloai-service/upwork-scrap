# ── Database (optional — defaults to local SQLite) ────────────────────────────
# Set DATABASE_URL to use Neon PostgreSQL instead of local SQLite.
# Get a free database at: https://neon.tech
# DATABASE_URL=postgresql://user:password@ep-xxx-xxx-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# ── AI Provider Keys ──────────────────────────────────────────────────────────

# Groq API Key (fallback provider, get free key at: https://console.groq.com/keys)
# Free tier: ~100K tokens/day
GROQ_API_KEY=your_groq_api_key_here

# Optional: XAI API Key for Grok AI (alternative provider)
# Get key at: https://x.ai/api
# XAI_API_KEY=xai-...

# ── Email Notifications ──────────────────────────────────────────────────────

# Gmail App Password for email notifications
# Generate at: https://myaccount.google.com/apppasswords
# Required only for sending proposal emails via SMTP
GMAIL_APP_PASSWORD=your_16_char_app_password_here

# ── Primary AI Provider (Qwen via Ollama remote) ────────────────────────────
# The primary AI provider is Ollama running Qwen on a remote server.
# Configure the SSH tunnel or base_url in config/ai_models.yaml.
# No API key needed for local/tunneled Ollama — uses dummy key "ollama".
#
# To set up SSH tunnel:
#   ssh -L 11434:localhost:11434 your-remote-server
#
# Or change base_url in config/ai_models.yaml to point directly at the server.
